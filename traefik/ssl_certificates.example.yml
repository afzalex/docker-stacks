tls:
  certificates:
    - certFile: "/etc/traefik/certs/fzryzen.local.crt"
      keyFile: "/etc/traefik/certs/fzryzen.local.key"

  stores:
    default:
      defaultCertificate:
        certFile: "/etc/traefik/certs/fzryzen.local.crt"
        keyFile: "/etc/traefik/certs/fzryzen.local.key"